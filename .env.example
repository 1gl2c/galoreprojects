# Single launch switch. Keep false until domain verification and delivery checks.
INQUIRY_FORM_ENABLED=false
# Store real values only in the hosting dashboard, never in git or chat.
RESEND_API_KEY=
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
# Random, stable secret, at least 32 characters. Used for privacy and idempotency.
INQUIRY_HASH_SECRET=
